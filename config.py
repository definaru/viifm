from datetime import timedelta

rules_url = 'https://viifm.art/rules'

mat_mut = timedelta(days=2)
mut_limit = 3

animation_mut = timedelta(hours=2)
animation_flood_limit = 8
animation_time_limit = timedelta(seconds=60)

site_mut = timedelta(days=1)
site_limit = 5


